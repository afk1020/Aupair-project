import React from 'react'
import AuPair from '../containers/AuPair';

const ListContainer = (props) => {
	return (
		<div>
			{/* {props.listings.map((listing) => <AuPair listing={listing} key={listing.id} handleLikeClick={props.handleLikeClick}/>)} */}
		</div>
	)
}

export default ListContainer;




